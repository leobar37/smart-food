import { Theme } from '@chakra-ui/react';

const breakpoints: Theme['breakpoints'] = {
  base: '0em',
  sm: '30em',
  md: '48em',
  lg: '62em',
  xl: '80em',
  '2xl': '96em',
};

export default breakpoints;